{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "EquipmentSlotRecord",
  "type": "object",
  "required": ["id", "category", "maxItems"],
  "properties": {
    "id": { "type": "string" },
    "category": { "type": "string" },
    "maxItems": { "type": "integer", "minimum": 1 }
  }
}