# Current GPT Handoff

Date: 2026-08-13

Status: `Version 0.6.10.3` complete with `REPAIR_REQUIRED`; parent `0.6.10` remains unaccepted

Active route:

`Version 0.6.10.4 - Ashen Reef Survey Progression Coherence And Projection Placement Repair`

Parent:

`Version 0.6.10 - Ashen Reef Survey Advancement Authority`

Exact authority chain:

- implementation: `008db9c93eb8818aea51652be07fd196df41c45f`;
- first repair: `59af92629a79e95fa20247959159e336a8dbc88e`;
- `0.6.10.3` starting head: `bc9783803c08ab403cad0302727d5b701291da40`;
- `0.6.10.3` audit authority: `ad4a080acc3d05a4a790c4b219780db11a1be1a1`;
- final coordination: resolve from pushed `master` HEAD; its exact identity is reported in the completion response because the commit cannot contain its own SHA.

## Immediate Guardrails

The next implementation owns only two residual findings:

1. `AR-007`: recompute retained progression/Echo authority from retained attributes, skills, and progression through the authoritative resolver before durable duplicate lookup. Internal arithmetic and recomputed canonical strings are not sufficient.
2. `AR-008`: inspect and repair exact placement of byte-correct survey-known notification and Chronicle rows under total `(appliedTick, resultId)` order. Preserve opaque row bytes and indices, caps, retention expiry, event pending-only rules, correction blocks, and repair idempotency.

Do not reopen the four passing `AR-002`, `AR-004`, `AR-005`, or `AR-006` surfaces without direct evidence. Do not broaden into UI/context/gameplay-loop, generic projections, save-version/migration, defeat/recovery, content, rewards/turn-in, geographic Knowledge implementation, balance, dependencies, or creator-to-survey reachability.

Use the tracked TS/JS mirrors consistently. Preserve the accepted two-tick survey behavior, nine explicit non-proposals, exact correction evidence, current caller identity semantics, accepted-only state application, and applied/blocked/unchanged panel facts.

The implementation outcome is only `IMPLEMENTED_PENDING_REAUDIT` or `REPAIR_INCOMPLETE`. It cannot accept the parent or classify representative-loop evidence. On success it must install separate production-read-only `Version 0.6.10.5 - Ashen Reef Survey Progression And Projection Post-Repair Acceptance Audit`.

`0.7.0`: `NOT_READY` pending parent acceptance and a later explicit docs-first band-entry decision.

## Validation Baseline

`0.6.10.3` reproduced:

- prescribed focused/adjacent grouping `177/177`;
- Knowledge `132/132`;
- clock/schema `107/107`;
- additional account/run/legacy/deterministic coverage `36/36`;
- Vite `5.4.21`, 212 modules, pass with the registered chunk advisory;
- bounded TypeScript exact baseline of 137 diagnostics, including only the same two pre-existing `ActivityPanel` TS2375 findings among repair-touched production files.

Green counts did not close the two adversarial failures. The repair must add owner-recomputation and destination-placement regressions in-process and after publication/restart.

## Branch And Evidence Posture

Fresh `0.6.10.3` orientation found one local branch, 36 non-default remote branches, and two open pull requests. PR #2 remains open non-draft at `e78dc645cfb658685be12f45f46d34b7c0da1119`; PR #3 remains open draft at `10afdef7d85a3010b5afadd20c0cd014ceac5fcc`. Both remain `SUPERSEDED_PRESERVE_EVIDENCE`.

Read-only evidence refs remain:

- activity: `b4cbaea5f4292904bba62f60a0108bb84f2bd405`;
- progression: `387f2491d0d671ee7834656c28183e72a798f1ca`;
- Chronicle/projections: `4b65d7fb6e093743cdc4d5a6db5b0e1bb6638c01`;
- Knowledge/discovery: `46434f31f8b06d49aad9a516543fbe36d188d519`;
- protected readiness: `59c103c3a06d55f35bffa735fd4b7814dffb583e`.

The four Connector refs remain `CANDIDATE_INTEGRATION` only for broader named consumers. Older blanket claims that survey advancement is UI-owned or lacks receipts are stale for the bounded survey owner; their generic evidence-class warnings remain reusable. The readiness ref remains `PROTECTED_REFERENCE`; its evidence-gated principle remains useful, while its missing-survey-implementation claims are stale. No lifecycle action was due. Reinspect at `0.6.10.4` orientation.

## Connector-Safe Follow-Up

No routine Connector intervention is required before `0.6.10.4`. A future Connector pass may inspect documentation-only coordination after the repair, but it must not modify the active production/test surface or claim local validation.
