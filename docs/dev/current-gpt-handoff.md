# Current GPT Handoff

Date: 2026-08-06

Status: parent `0.6.9` acceptance reopened; historical recovery-fork authority decision active; twenty-eight isolated Connector evidence lanes indexed; token-reset prestage complete; bounded Codex/Connector segmentation policy installed; one deferred character-creator asset-variant issue recorded

## Current Route

- Latest implemented primary: `Version 0.6.9 - Normal Stakes Campaign Persistence Foundation`.
- Parent status: `ACCEPTANCE_REOPENED_PENDING_FOCUSED_DECISION`.
- Existing lineage implementation: `cbad987028d81c5ecdc35403333ec920d0ea5e53`.
- Implementation status: `IMPLEMENTED_PENDING_AUTHORITY_RECONCILIATION_AND_REAUDIT`.
- Historical `0.6.9.10` acceptance: superseded pending a properly installed successor.
- Active route: unversioned `Historical Recovery Fork Evidence Verifiability And Parent Acceptance Reopening Decision`.
- Hardened prompt commit: `11a18dbeb373d024907f1e0f18ab1716b701930c`.
- Ashen Reef survey decision: blocked.
- Survey implementation: unimplemented.
- `0.7.0`: `NOT_READY`.

## Active Prompt Rules

The active prompt:

1. reserves historical `0.6.9.10`;
2. uses `0.6.9.11` for the next audit or repair;
3. reserves `0.6.9.12` for a post-repair audit when repair is required;
4. treats the repository-first protocol's old `Current Application` paragraph as a stale pointer;
5. distinguishes semantic consistency from authenticated or cryptographic tamper detection;
6. prohibits integration or modification of `parallel/*` evidence branches during the active decision.

The active decision may not accept parent `0.6.9`, edit or revert production, edit tests, implement Ashen Reef, or absorb unrelated deferred issues.

## Token-Reset Waiting-Period Prestage

Read during the next Codex orientation:

`docs/dev/connector-token-reset-waiting-period-prestage-2026-08-06.md`

Publication commit:

`93a97f92062b58ecd929a05a4aa4dc3dd7c50a49`

The prestage provides:

- a hosted branch and pull-request snapshot;
- current comparisons for the eight non-evidence branches;
- discovery pointers for all twenty-eight indexed evidence branches;
- an active-decision source map;
- a C0 → C1 → C2 → C3 characterization matrix;
- a six-field historical-edge evidence worksheet;
- a Codex resume checklist.

PR #3's body was corrected from stale `HOLD_NAMED_CONSUMER` wording to `SUPERSEDED_PRESERVE_EVIDENCE`. The PR remains open and draft; its branch and lifecycle state did not change.

The prestage is Connector evidence only. Codex must fetch, prune, synchronize, and refresh all dynamic facts in the authenticated checkout.

## Dynamic Platform And Tool Routing

Durable policy:

`docs/dev/prompt-execution-platform-tool-selection-policy.md`

Before every prompt is created, revised, or presented, perform a fresh capability inventory covering:

- regular ChatGPT;
- Deep Research;
- ChatGPT via GitHub Connector;
- Agent Mode;
- GPT Work or successor workspaces;
- current Codex modes, environments, models, and reasoning levels;
- installed and potentially installable plugins, connectors, skills, and tools;
- repository, worktree, web, application, execution, validation, commit, and push access;
- token/quota, quality, latency, privacy, and coordination tradeoffs;
- capabilities that cannot operate together on one surface.

Required access and completion authority are hard gates. The fresh platform/mode/model/tool/plugin recommendation belongs outside the copy-paste prompt body.

The user currently reports that Deep Research can provide external depth without consuming Codex or GPT Work tokens but cannot receive private GitHub access on that surface. Re-verify this whenever capabilities change. Use an explicit staged workflow when both external research and private-repository execution are required.

## Codex And Connector Segmentation Policy

Durable companion policy:

`docs/dev/codex-connector-segmentation-and-independent-review-policy.md`

Policy commit:

`c0fc4f175b66a21c8b14314ff113411fc4570ba7`

### Default ceiling

A normal substantial run should use no more than:

1. one optional Connector or research prepass;
2. one complete, uninterrupted Codex run;
3. one optional Connector independent post-run review.

Do not insert routine Connector checkpoints in the middle of Codex implementation. Connector findings should be batched into one review package and, when source or executable work is required, one consolidated follow-up prompt.

### Connector-owned work Codex normally need not repeat

Connector may fully own current and complete:

- remote branch, PR, commit, and issue inventory;
- focused source/owner/caller maps;
- prompt hardening and acceptance matrices;
- cross-document consistency and stale-pointer audits;
- evidence indexing and named-consumer registration;
- external research briefs that do not claim private-repository access;
- PR metadata corrections;
- tiny complete-file documentation corrections;
- independent post-run prompt-compliance and claim-to-evidence review.

Codex should refresh dynamic facts before acting but should not recreate the entire stable artifact without a freshness or independent-verification reason.

### Work that remains inside Codex

Keep one coherent package for:

- repository synchronization, local branches, history, and worktree truth;
- implementation, repair, executable decision work, tests, and temporary probes;
- builds, typechecks, lint, generators, simulations, browser checks, and generated output;
- source/test/schema/migration/content/asset/dependency coordination;
- large shared coordination reconciliation driven by the run outcome;
- run-specific output based on commands actually executed;
- final diff/status review, commit, push, and live-head verification;
- acceptance or parent promotion requiring executable proof.

### Current route application

The optional prepass is already complete through the waiting-period prestage. No additional prepass or mid-run Connector checkpoint is required. After Codex commits and pushes the active decision and exact successor, perform one Connector independent review of the committed package. Return only one consolidated Codex follow-up if source, executable evidence, acceptance, or large coordination work remains.

## New Deferred Issue Inventory

Connector triage:

`docs/dev/connector-current-untracked-bug-and-issue-triage-2026-08-06.md`

Publication commit:

`0da777605d54f3547fa1b6054f8449651154be2e`

Disposition:

`HOLD_NAMED_CONSUMER_CHARACTER_CREATOR_VISUAL_ASSET_UI_CONTRACT_PASS`

### LR-UI-ASSET-001

All nine character-creator continent `*-selected.png` files are byte-identical to their normal counterparts, while the catalog and UI maintain and consume distinct selected-image paths.

Confirmed narrow effect:

- the selected-image variant channel cannot provide distinct image content;
- this does not prove that selection lacks CSS, text, focus, or accessible state;
- product intent is unresolved: distinct selected artwork may be unfinished, or CSS may be intended to own selection and the duplicate path contract may be unnecessary.

Do not widen the active persistence decision to fix this issue. A later character-creator visual asset/UI contract pass should choose between distinct selected artwork and removal of the redundant variant contract, then run local build, browser, responsive, and accessibility validation.

The Connector triage found no open GitHub issues, indexed `TODO`/`FIXME` markers, indexed skipped-test markers, or indexed `Math.random`/`Date.now` use. Search absence is not proof of complete defect absence; no second issue met the threshold for a new Connector-confirmed finding.

## Connector Evidence Indexes

### First wave

`docs/dev/parallel-gpt-connector-only-run-results-2026-08-03.md`

Domains: consumables, equipment profiles, gameplay shell, quest turn-in, production, bestiary/ecology, settlements, research artifacts, rest/recovery, and text-first combat presentation.

### Second wave

`docs/dev/parallel-gpt-connector-only-second-wave-results-2026-08-03.md`

Domains: economy commands, progression rewards, Chronicle/notification provenance, accessibility, NPC parties, item-use ownership, travel/services, Knowledge visibility, JS/TS module integrity, and content validation.

### Third wave

`docs/dev/parallel-gpt-connector-only-third-wave-results-2026-08-03.md`

Domains: combat AI, magic readiness, activity advancement, quest action trees, civilization simulation, world spawn admission, game-tick orchestration, and character-panel mutation.

All twenty-eight evidence branches remain isolated, unmerged, and noncontrolling as `CANDIDATE_INTEGRATION`.

## Mandatory Discovery Rule

A future run must inspect semantically applicable entries when it:

1. names a mandatory consumer from an index;
2. changes a covered file, owner, contract, behavior, or verification surface;
3. claims the domain is mature, complete, engine-owned, replay-safe, deterministic, accessible, party-ready, effect-ready, world-integrated, provenance-aware, module-integrated, content-validated, presentation-only, simulation-mature, migration-ready, or deletion-ready;
4. proposes integrating, superseding, retiring, or deleting an evidence branch;
5. relies on the domain for a representative-loop or milestone audit.

Completion reports must identify applicable entries and state whether they were inspected. Unrelated entries may be omitted. Evidence branches never override newer accepted authority.

## Branch Lifecycle

Do not merge evidence waves as batches.

At a named consumer or dedicated coordinator:

- resolve the exact branch head;
- compare against then-current `master`;
- classify findings as current, superseded, narrowed, or historical;
- integrate, re-author, hold, or retire independently;
- verify equivalent preservation before deletion.

The large `docs/dev/branch-disposition-register.md` remains a 2026-08-02 snapshot. The active decision or a later repository-capable coordinator must reconcile it once from complete live evidence. Until then, this handoff, the waiting-period prestage, and the three result indexes are the controlling discovery pointers.

## Validation Posture

The evidence audits, routing policies, waiting-period prestage, segmentation policy, and current issue triage used GitHub Connector inspection and complete-file documentation writes only.

No local tests, builds, typechecks, lint, generators, simulations, browser checks, worktree commands, image decoding, or executable lineage probes were run.

## Next Action

After token reset:

1. perform the fresh platform/tool/model/plugin capability inventory;
2. run the exact prompt in `docs/dev/current-codex-prompt.md` from a freshly synchronized authenticated checkout;
3. read and verify the waiting-period prestage during orientation;
4. execute the historical fork decision end to end without a routine Connector interruption;
5. reconcile the complete branch register and required coordination documents from the decision outcome;
6. install the exact `0.6.9.11` successor and stop without accepting the parent inside the decision route;
7. after the committed result is pushed, run one Connector independent post-run review under the segmentation policy.
