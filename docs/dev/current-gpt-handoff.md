# Current GPT Handoff

Date: 2026-08-03

Status: parent `0.6.9` acceptance reopened; hardened historical recovery-fork authority decision active; ten isolated connector-only evidence lanes validated

## Current Route

- Latest implemented primary: `Version 0.6.9 - Normal Stakes Campaign Persistence Foundation`.
- Current parent status: `ACCEPTANCE_REOPENED_PENDING_FOCUSED_DECISION`.
- `0.6.9.8` remains useful historical evidence with conclusion `REPAIR_REQUIRED`.
- Existing lineage implementation: `cbad987028d81c5ecdc35403333ec920d0ea5e53`.
- Existing implementation status: `IMPLEMENTED_PENDING_AUTHORITY_RECONCILIATION_AND_REAUDIT`.
- Prior claimed `0.6.9.10` parent acceptance: superseded pending the active decision and a properly installed successor.
- Active route: unversioned `Historical Recovery Fork Evidence Verifiability And Parent Acceptance Reopening Decision`.
- Hardened prompt commit: `11a18dbeb373d024907f1e0f18ab1716b701930c`.
- Parallel-run inventory: `docs/dev/parallel-gpt-connector-only-run-candidates-2026-08-03.md`.
- Parallel-run inventory commit: `3bc3093e46966a9bd85b4df14ceba9eb0427177c`.
- Current output commit: `5b2da5b19f9c5124c815c577588ab8badce27f3c`.
- Ashen Reef survey receipt-foundation decision: blocked.
- Survey behavior remains unimplemented.
- `0.7.0` remains `NOT_READY`.

## Prompt Hardening

The active prompt now corrects five execution risks:

1. it assigns exact successor labels rather than referring vaguely to the next suffix;
2. it reserves historical `0.6.9.10` and uses `0.6.9.11` next;
3. it identifies the stale `Current Application` paragraph in the repository-first protocol without weakening the rest of that protocol;
4. it distinguishes semantic consistency validation from independently authenticated or cryptographic tamper detection;
5. it prohibits integration or modification of parallel connector branches during the active decision.

Exact successor rules:

- conforming implementation -> `Version 0.6.9.11 - Historical Recovery Fork Authority Acceptance Audit`;
- repair/replacement required -> `Version 0.6.9.11 - Historical Recovery Fork Authority Repair`;
- reserved independent successor -> `Version 0.6.9.12 - Historical Recovery Fork Authority Post-Repair Acceptance Audit`.

The decision itself still may not accept parent `0.6.9`, edit or revert production, edit tests, or run Ashen Reef.

## Active Decision Requirements

The active Codex run must:

- reconstruct prompt/handoff/output state at `5fe11910`, `551d14bc`, `cbad987`, and `f68d878`;
- confirm the skipped executable-prompt transitions;
- classify every historical fork field by actual durable proof;
- execute recovery-edge, intermediate-edge, current-edge, cycle, duplicate, disconnected-path, wrong-recovery, and original-continuity-substitution probes;
- decide a continuity-graph, fully authenticated, or bounded mixed authority model;
- avoid claiming universal historical tamper detection unless persisted authority proves it;
- classify `cbad987`;
- install the exact `0.6.9.11` successor appropriate to the decision;
- update the stale protocol current-application paragraph and all affected live coordination surfaces only after the decision.

## Ten Parallel GPT Connector-Only Evidence Lanes

The following may run concurrently because each is one-document, connector-only, read-only evidence work on its existing isolated branch:

1. `parallel/consumable-profile-coverage-audit`;
2. `parallel/equipment-profile-readiness-audit`;
3. `parallel/gameplay-shell-ui-state-audit`;
4. `parallel/quest-turn-in-reward-source-audit`;
5. `parallel/recipe-production-maturity-audit`;
6. `parallel/regional-bestiary-ecology-maturity-audit`;
7. `parallel/regional-settlement-maturity-audit`;
8. `parallel/research-artifact-retention-inventory`;
9. `parallel/rest-recovery-mutation-audit`;
10. `parallel/text-first-combat-view-model-audit`.

Shared restrictions:

- refresh against live master and record its SHA;
- update only the branch's one existing unique document;
- do not touch current prompt/output/handoff, roadmap, sequence, planning anchor, backlog, static program, branch register, production, tests, schemas, content, assets, or dependencies;
- do not claim local validation;
- remain isolated and unmerged;
- report a named integration/review trigger.

One coordinator should update the branch register after all ten complete. The ten runs must not concurrently edit that shared register.

## Parallel Candidates Held Back

- `parallel/connector-prep-freshness-audit`: overlaps active route and coordination files.
- `parallel/prompt-packaging-integrity-audit`: protected prompt-packaging reference and direct prompt overlap.
- `parallel/launcher-asset-pr-disposition`: optional eleventh pass; lower priority because PR #2 already has a durable evidence-only disposition and advancement requires a user-facing asset decision plus local UI/asset validation.

## Branch And PR Posture

- PR #2 remains open, unmerged, non-draft, and mechanically non-mergeable at `e78dc645cfb658685be12f45f46d34b7c0da1119`.
- PR #3 remains open, draft, unmerged, and mechanically non-mergeable at `10afdef7d85a3010b5afadd20c0cd014ceac5fcc`.
- No branch or PR lifecycle mutation occurred.
- The active decision must re-fetch and reclassify live branches before relying on this snapshot.

## Validation Posture

The connector inspection did not run local tests, builds, typechecks, or executable lineage probes.

It did verify live commits, open PRs, branch names, candidate branch changed paths, selected branch-document scopes, current policy documents, and complete-file writes through the GitHub Connector.

Hosted status must be checked again for the final handoff head; the prior inspected head had no combined statuses.

## Next Action

Run the exact current prompt in `docs/dev/current-codex-prompt.md` from a freshly fetched synchronized checkout containing at least:

`11a18dbeb373d024907f1e0f18ab1716b701930c`

The parallel connector-only lanes may run at the same time under the isolation contract in `docs/dev/parallel-gpt-connector-only-run-candidates-2026-08-03.md`.
