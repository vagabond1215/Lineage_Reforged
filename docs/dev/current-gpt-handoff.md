# Current GPT Handoff

Date: 2026-08-27

Status: `Version 0.6.11` implemented; independent parent/representative audit remains the next executable action

Repository: `vagabond1215/Lineage_Reforged`

Hosted master before this handoff write:

`70108617fa2902dbac1fef340d59381e95bf7d96`

Implementation disposition:

`IMPLEMENTED_PENDING_PARENT_AUDIT`

Implementation commit:

`3ca23d6864541a899ea61a6bf26257665f754e78`

Active run:

`Version 0.6.11.1 - Ashen Reef Survey Ordinary Reachability And Representative Loop Acceptance Audit`

Current prompt authority:

`docs/dev/current-codex-prompt.md`

Current prompt blob SHA:

`064749af0435e839df71fe4619ccc30d7ce4ff35`

Representative classification:

`REPRESENTATIVE_LOOP_EVIDENCE_INCOMPLETE`

`0.7.0`:

`NOT_READY`

## 1. Immediate Next Action

The next repository-capable run should be the installed production-read-only `0.6.11.1` independent audit.

Do **not** open another speculative Connector pass before that audit unless:

- Codex cannot start;
- a new blocking repository fact appears;
- the user explicitly asks for another bounded docs-only pass;
- or the active prompt must be repaired because its authority changed.

The Connector has reached the safe preparation boundary for this parent.

Codex must independently decide:

- `PARENT_ACCEPTED`; or
- `REPAIR_REQUIRED`.

Connector preparation must not prejudge that result.

## 2. Resource-Constrained Execution Procedure

The current durable division of labor is now governed by:

- `docs/dev/codex-vs-gpt-connector-handling-procedure.md`;
- `docs/dev/codex-resource-budget-and-execution-slicing-policy.md`;
- `docs/dev/codex-connector-segmentation-and-independent-review-policy.md`;
- `docs/dev/gpt-connector-assistance-policy.md`;
- `docs/dev/prompt-execution-platform-tool-selection-policy.md`.

Root `AGENTS.md` now points directly to the handling procedure.

### Required operating posture

When Codex/high-reasoning capacity is constrained:

1. GPT/Connector should perform all useful safe repository discovery, authority reconciliation, branch/PR inventory, source/caller/test mapping, product-question closure, prompt hardening, and documentation work first.
2. Multiple bounded Connector preparation passes are allowed when each pass materially reduces Codex discovery, package size, or interruption risk.
3. There is no longer an arbitrary one-prepass ceiling.
4. Once Codex starts, avoid routine mid-run GPT/Codex ping-pong. Give Codex one coherent local/executable slice.
5. Use the strongest/highest-cost reasoning tier only for slices that genuinely need it.
6. If the strongest tier is required, narrow the slice rather than assigning it more discovery.
7. Prefer `XS`/`S` implementation units under severe short-window constraints; `M` splits by default; `L`/`XL` must be decomposed unless atomicity or explicit user approval justifies otherwise.
8. Name an early durable checkpoint.
9. After Codex pushes, GPT/Connector should inspect the exact hosted result and batch any findings into one consolidated review/follow-up.
10. Treat user-observed short-window limits as current planning constraints, not as permanent product-limit facts.

If a Codex run is interrupted:
- preserve the same thread/worktree when possible;
- inspect HEAD/status/diff/commits before resuming;
- if valid partial work exists, continue from the first incomplete requirement;
- if the worktree is clean and substantive orientation context was lost, stop rather than repeat broad discovery and return to Connector decomposition.

## 3. Current Audit Orientation Packet

The current Connector preflight packet is:

`docs/dev/connector-preflight-0.6.11.1-acceptance-audit-evidence-packet.md`

It was prepared against the earlier exact hosted head:

`cfb569c7f196e71e2f63451dc583f54ad31cd0b2`

Since then, Connector work has changed documentation only.

Codex should:

1. verify its synchronized local head;
2. read the packet;
3. inspect the complete delta from `cfb569c7...` to its local current head;
4. confirm that the post-packet delta is documentation/workflow-only;
5. independently reverify every packet claim material to acceptance;
6. avoid broad repository rediscovery merely to reproduce file locations, branch inventories, and source maps already supplied.

The packet is orientation evidence only. It cannot establish acceptance.

## 4. Implementation State To Audit

The accepted parent before this package remains:

`Version 0.6.10 - Ashen Reef Survey Advancement Authority`

Parent acceptance authority:

`950e851446fb75bfbdb717d0ea33e33ec2907d4a`

The `0.6.11` implementation adds:

- canonical authored definition `quest_definition.starfall_ashen_reef_soundings`;
- player-facing title `Soundings of Ashen Reef`;
- ordinary Starfall new-campaign offer staging before initial publication;
- no access grant merely from offer presence;
- acceptance-owned exact settlement-less Ashen access;
- accepted quest tracking;
- Starfall versus Survey Anchorage travel-origin correction;
- survey-content version 2 while retaining exact v1/mixed-history compatibility;
- injection-free creator -> publication -> acceptance -> travel -> four-shift -> restart -> durable duplicate representative evidence.

The expected representative terminal field-work state remains:

- 4 ordered accepted requests;
- 4 occurrences;
- 4 results;
- 48 survey consequence receipts;
- 3 sectors plus ruins;
- complete survey operation;
- return activity;
- active, unturned-in quest;
- no ordinary correction/repair;
- no quest payout;
- no turn-in reward delivery;
- no currency/standing/inventory reward;
- no Geographic Knowledge grant;
- no extra General Lore turn-in effect.

These are implementation claims until `0.6.11.1` independently proves them.

## 5. Audit Priorities

Codex should spend its constrained execution window on executable evidence, not repository archaeology.

Priority order:

1. verify local repository identity, synchronized HEAD/upstream, clean/understood worktree, active prompt, and documentation-only delta since the Connector packet;
2. independently audit authored definition/schema/type widening;
3. independently test offer staging/publication/retry/restart semantics;
4. independently test acceptance-owned tracking + exact Ashen access atomicity;
5. independently test Starfall/Ashen travel identity;
6. adversarially verify v1/v2/mixed survey-content persistence, duplicate, correction/repair, projection, discovery/Codex semantics;
7. independently execute the production creator -> publish/load -> accept/admit -> restart -> travel/admit -> four real caller shifts -> restart -> durable duplicate path without fixture eligibility injection;
8. rerun the prompt-required focused/adjacent matrix, lint, build, bounded TypeScript baseline, mirrors/exports, persistence/recovery gates, diff/hygiene, branch/PR and hosted identity checks;
9. return exactly the prompt-defined acceptance or repair disposition.

Do not accept from the existing `1072/1072`, `24/24`, lint/build, or 137-diagnostic implementation report alone. Those are baselines to reproduce/adversarially challenge.

## 6. Connector Work Completed Since The Audit Preflight

All work below is documentation-only and does **not** change the `0.6.11.1` acceptance criteria.

### Pass 7 — Attribute / Skill / Ability Responsibility Audit

Files:

- `docs/design/attribute-skill-ability-responsibility-audit.md`;
- `docs/dev/connector-safe-pass-7-attribute-skill-ability-responsibility-audit-plan.md`.

Result:

`AUDIT_COMPLETE_PRESERVE_NINE_PENDING_FOCUSED_DESIGN`

Key outcome:
- preserve all nine primary attributes;
- distinguish authored governing metadata from actual runtime formulas;
- future focused reconciliation is needed for resource metadata/runtime drift and underwired INT/SPT/CHA growth sources.

### Pass 8 — Classless Progression / Placeholder Provenance Audit

Files:

- `docs/design/classless-progression-and-placeholder-provenance-audit.md`;
- Pass 8 plan.

Result:

`AUDIT_COMPLETE_CLASSLESS_DIRECTION_CONFIRMED_COMPATIBILITY_RETAINED`

Key outcome:
- ordinary player creation is classless: `classId: null`, `jobId: null`, class level `0`;
- retained class fields/profiles are live compatibility scaffolding and require a future migration/retirement package;
- current skill/ability/spell schemas and inspected records are not class/job-gated;
- current traits are lineage-sourced.

### Pass 9 — Player Stats Data Dictionary Repair

Files:

- `docs/data-dictionary/player-stats.md`;
- Pass 9 plan.

Result:

`DOC_REPAIR_COMPLETE`

Key outcome:
- removed stale current-state claims about job traits/job abilities/future class taxonomy;
- documented live classless model, Echo, stat growth, resource authority drift, and compatibility fields;
- corrected storage table name to live `player_progression_tracks`.

### Pass 10 — Quest Turn-In And Reward Readiness Audit

Files:

- `docs/design/quest-turn-in-and-reward-readiness-audit.md`;
- Pass 10 plan.

Result:

`AUDIT_COMPLETE_OWNER_CONTRACT_READY_AFTER_PARENT_ACCEPTANCE_AND_AUTHORED_TERMS`

Key outcome:
- current turn-in remains a UI/game-shell `legacy_bridge`;
- old Saltmere Ashen reward constants are characterization only and **not Soundings canon**;
- field-work completion, turn-in, quest completion, consequence delivery, and projection must remain separate;
- a first future engine turn-in should be quest-specific, not a generic reward framework;
- implementation is gated by `0.6.11.1` acceptance and explicit authored Soundings turn-in terms.

Do not widen the active audit into any of these future systems.

## 7. Workflow Procedure Update Completed Before Audit

Documentation-only workflow commits before this handoff include:

- `c23e5b372e8e589c8bb90de28bd614c3fa13964a` — add Codex versus GPT Connector handling procedure;
- `c2b8b3cd5c89053419543935c96ed3a40744dcff` — make segmentation resource-aware;
- `d49f293ebfffc8f1f68a87ee8bcce459ca6bb305` — remove stale fixed Codex mode/current-route routing;
- `f858d4f6b78792274116a4f940da16e979461497` — align GPT repository operating guide;
- `7c8a65d9ef8e6553afc03afe392ec0f146be533a` — route root agents through the new handling procedure;
- `70108617fa2902dbac1fef340d59381e95bf7d96` — link proactive Connector assistance to the procedure.

These commits alter workflow/documentation only.

## 8. Scope Guardrails

Do not reopen during `0.6.11.1`:

- survey turn-in/payout/rewards;
- legacy Saltmere reward constants;
- generic quest reward infrastructure;
- attribute rebalance/merge;
- class compatibility-field removal;
- generic quest/mission/order/favor runtime semantics;
- broad travel-id migration;
- Geographic Knowledge/map/fog access;
- other Stakes modes;
- checkpoint/cloud/death/succession;
- broad TypeScript cleanup;
- direct `0.7.0` implementation.

The audit is production-read-only except removable probes allowed by the installed prompt.

## 9. Branch / PR Posture

Latest Connector branch-disposition audit found:

- 38 hosted branches total: `master` + 37 non-default;
- exactly two open PRs;
- PR #2 and PR #3 remain `SUPERSEDED_PRESERVE_EVIDENCE`;
- four survey evidence refs remain `CANDIDATE_INTEGRATION`;
- integrated-gameplay readiness remains `PROTECTED_REFERENCE`;
- administration evidence remains `HOLD_NAMED_CONSUMER`.

Codex must still perform the fresh branch/PR/ref verification required by its active prompt. Do not mutate protected/evidence refs merely because they are old.

## 10. Expected Audit Outcomes

### If `PARENT_ACCEPTED`

Codex should:
- complete the permanent acceptance coordination required by the prompt;
- issue `REPRESENTATIVE_LOOP_ACCEPTED`;
- keep `0.7.0` `NOT_READY`;
- install the separate docs-first Integrated Gameplay `0.7` Band-Entry Readiness Decision.

After Codex pushes, the next GPT thread should inspect the exact hosted audit result and then Connector-preflight the band-entry decision before recommending another expensive Codex run.

### If `REPAIR_REQUIRED`

Codex should:
- not repair production inside the independent audit;
- record exact numbered findings;
- install only the smallest parent-specific `Version 0.6.11.2` repair;
- keep representative evidence incomplete and `0.7.0` not ready.

After Codex pushes, the next GPT thread should inspect the exact findings and use Connector preparation to reduce the repair to the smallest `XS`/`S` coherent Codex slice where possible.

## 11. New GPT Thread Start Procedure

A new GPT thread should begin by reading:

1. `AGENTS.md`;
2. `docs/dev/current-gpt-handoff.md`;
3. `docs/dev/current-codex-prompt.md`;
4. `docs/dev/current-codex-output.md`;
5. `docs/dev/codex-vs-gpt-connector-handling-procedure.md`;
6. `docs/dev/codex-resource-budget-and-execution-slicing-policy.md`;
7. `docs/dev/connector-preflight-0.6.11.1-acceptance-audit-evidence-packet.md`.

Then fetch live `master` and compare it with the hosted head named above.

Before the next Codex audit begins, the GPT thread should not install a different prompt or broaden the route unless live repository evidence shows that the current prompt is stale or invalid.
