# Connector Prep Freshness And Supersession Audit

Date: 2026-07-29

Source route: ChatGPT via GitHub Connector

Source commit: `3006c968eb40b1d72f64fb2dc0263e227f869a7d`

Status: connector-only, read-only document audit; no deletion, roadmap advancement, prompt installation, implementation, or runtime change

## 1. Purpose

Classify connector-authored planning, source-map, audit, and workflow documents so future GPT and Codex runs do not mistake stale execution pointers for current authority.

This audit distinguishes:

- current durable authority;
- current supporting evidence;
- useful but partially superseded prep;
- fully superseded execution guidance retained for history;
- cleanup or fold candidates requiring later confirmation;
- isolated branch and pull-request material that must remain noncontrolling.

This document does not edit, fold, delete, merge, rebase, or promote any existing file or branch.

## 2. Current Execution Anchor

At the source commit:

- latest accepted primary: `Version 0.6.8 - Lethal-Process Definition Static Foundation`;
- latest completed support run: `Version 0.6.8.1 - Lethal-Process Definition Static Foundation Acceptance Audit`;
- latest completed unversioned run: `Post-Lethal-Process Static Foundation Next-Capability Classification Gate`;
- active route: unversioned `Ashen Reef Survey Activity Advancement Scope And Owner Contract Decision`;
- mutable health remains `NO_PACKAGE`;
- `0.7.0` remains `NOT_READY`.

Current execution precedence remains:

1. `docs/dev/current-codex-prompt.md`;
2. `docs/dev/current-gpt-handoff.md`;
3. `docs/dev/current-codex-output.md`;
4. `docs/dev/historical-version-and-deferred-route-register.md`;
5. the most specific focused decision;
6. `docs/design/current-planning-anchor-reconciliation.md`;
7. roadmap and sequenced plan for nonconflicting context.

Any connector-prep document that names an older active version or route is automatically noncontrolling.

## 3. Classification Vocabulary

| Classification | Meaning |
| --- | --- |
| `CURRENT_DURABLE_AUTHORITY` | May be used for its stated design or operating rules, subject to current precedence. |
| `CURRENT_SUPPORTING_SOURCE` | Useful evidence or source mapping; does not set the active route. |
| `PARTIALLY_SUPERSEDED` | Core analysis remains useful, but current-state, sequencing, counts, or owner posture must be refreshed. |
| `HISTORICAL_ONLY` | Retained to explain prior decisions; must not drive current execution. |
| `FOLD_DELETE_CANDIDATE` | Appears consumed, but deletion requires a separate reference and retention check. |
| `ISOLATED_NONCONTROLLING` | Exists on a branch or PR and must not be merged or treated as master authority without a dedicated review. |

## 4. High-Priority Freshness Findings

### 4.1 Current prep index

File:

`docs/design/current-prep-index-and-codex-source-stack.md`

Classification:

`PARTIALLY_SUPERSEDED`

Useful retained content:

- compact grouping of Knowledge, runtime/state/events, family/legacy/magic/combat, economy/travel/map/settlement, and UI/asset prep stacks;
- durable warning that an index is not implementation authority;
- source-selection guidance to use the smallest task-relevant stack;
- explicit prohibition on converting connector-only prep into runtime, schema, content, or roadmap advancement.

Stale content:

- date and active anchor remain tied to `Version 0.5.107 - Knowledge Domain Registry Plan`;
- stop-point language says to return to that old route;
- source stacks do not include the later accepted `0.6.4`-`0.6.8` decisions, health chain, Geography decision, Activity Resolution audit, or current survey route.

Disposition:

Retain as a historical source-stack index, but do not cite its active-version or stop-point sections as current. A later maintenance pass should either refresh it comprehensively or replace it with a shorter timeless source-family index.

### 4.2 Historical audit document index

File:

`docs/dev/historical-audit-doc-index.md`

Classification:

`PARTIALLY_SUPERSEDED`

Useful retained content:

- per-document successor mapping;
- fold/delete conditions;
- prohibition on using historical labels as active authority;
- cautious one- or two-file cleanup policy.

Stale content:

- current boundary still identifies `0.6.5` as active;
- cleanup instructions still refer to waiting for a stable `0.5.100` handoff;
- several status labels, especially the magic-chain and typecheck entries, require current verification before cleanup.

Disposition:

Retain as a supporting historical index. Do not use its active-boundary or timing statements. Refresh only in a dedicated complete-file maintenance pass.

### 4.3 Fold-ready candidate proposal

File:

`docs/dev/fold-ready-candidate-proposal.md`

Classification:

`HISTORICAL_ONLY`

Useful retained content:

- specific candidate analysis for the economy clarity audit, economy test-failure triage, and Chronicle run-end source audit;
- exact facts proposed for later folding;
- safe deletion prerequisites.

Stale content:

- active path references `Version 0.5.100`;
- proposed cleanup order predates the accepted `0.6.4`-`0.6.8` program;
- it cannot establish that current references are historical-only.

Disposition:

Keep until a new reference-check pass confirms the three candidates' current inbound references and durable retention needs. Do not delete from this audit.

### 4.4 Future system design ledger

File:

`docs/design/future-system-design-ledger.md`

Classification:

`CURRENT_DURABLE_AUTHORITY`

Useful current content:

- north-star criteria;
- ownership-before-behavior;
- evidence-before-access;
- presentation-before-mutation;
- current-data-first policy;
- draft-versus-runtime separation;
- durable vocabulary and system boundaries.

Freshness caution:

Its own authority and precedence section predates the current explicit prompt-first ordering. Use it for durable design intent, not exact repository state, active sequencing, or latest owner decisions.

Disposition:

Retain. Update only when durable design intent changes or when a carefully scoped maintenance pass reconciles its precedence wording.

## 5. Settlement And World Prep Stack

The following connector-authored documents remain useful current supporting sources for future regional maturity work:

- `docs/design/map-grid-distance-source-map.md`;
- `docs/design/travel-knowledge-route-source-map.md`;
- `docs/design/regional-population-center-expansion-audit.md`;
- `docs/design/settlement-placement-heuristics-plan.md`;
- `docs/design/settlement-expansion-content-shape-plan.md`;
- `docs/design/settlement-target-gap-report-plan.md`;
- `docs/design/pilot-region-selection-criteria-plan.md`.

Classification:

`CURRENT_SUPPORTING_SOURCE` with stale sequencing notes

The source maps, field inventories, non-mutation rules, target-gap shape, placement heuristics, content-shape requirements, and pilot criteria remain relevant.

Their recommendations to return to old `0.5.x` routes are superseded. They also predate accepted `0.6.4` settlement enrichment and the current regional-maturity finding, so all live counts must be reproduced before reuse.

## 6. Knowledge, Runtime, Family, Economy, UI, And Validation Prep

### Knowledge prep

Files such as:

- `docs/design/knowledge-framework-source-map.md`;
- `docs/design/knowledge-domain-backlog-normalization.md`;
- `docs/design/knowledge-discovery-source-vocabulary.md`;
- `docs/design/knowledge-registry-field-ownership.md`;
- `docs/design/knowledge-boundary-glossary.md`.

Classification:

`CURRENT_SUPPORTING_SOURCE`

They remain useful for vocabulary and owner boundaries but must be reconciled with the accepted Geography and location-recognition decision before any implementation route.

### Runtime/state/event prep

Files such as:

- `docs/design/runtime-state-ownership-ledger-prep.md`;
- `docs/design/advancement-event-boundary-audit.md`;
- `docs/design/chronicle-renown-evidence-boundary-map.md`;
- `docs/design/save-load-reliability-source-map.md`;
- `docs/design/validation-blocker-inventory.md`.

Classification:

`CURRENT_SUPPORTING_SOURCE`

These are especially relevant to the active survey owner-contract decision, but they do not override accepted travel, quest, activity-selection, health, persistence, or current survey evidence.

### Family, Legacy, Bloodlines, magic, and combat prep

Classification:

`CURRENT_SUPPORTING_SOURCE` or `PARTIALLY_SUPERSEDED` by focused owner decisions

Use the latest focused family, Bloodlines, magic, combat, health, and narrative decisions first. Older source maps remain evidence, not route authority.

### Economy and crafting prep

Classification:

`CURRENT_SUPPORTING_SOURCE`

The economy command surface and production source maps remain useful, but they predate accepted `0.6.5` and must not be used to infer crafting execution, inventory transactions, market stock, or dynamic pricing.

### UI and asset prep

Classification:

`CURRENT_SUPPORTING_SOURCE`

The shell and asset source maps may guide future presentation work. They do not authorize route, save, account, progression, image-path, or gameplay changes.

## 7. Temporary Research And Integration Artifacts

The repository contains surviving `docs/dev/tmp-*` research artifacts in several domains.

Classification:

`REQUIRES_SEPARATE_RETENTION_INVENTORY`

This audit does not classify individual temporary artifacts because safe disposition requires, for each file:

- source identity;
- integrity hash where authoritative;
- named consumers;
- completed and remaining consumers;
- durable conclusions retained elsewhere;
- active prompt references;
- explicit deletion condition.

No temporary artifact should be deleted merely because its name begins with `tmp-`.

## 8. Isolated Branches

### `prep/integrated-gameplay-0-7-readiness-audit`

Classification:

`ISOLATED_NONCONTROLLING`

It remains a read-only comparison source. It is materially behind current `master`, and its contents must not be merged, rebased, deleted, or treated as current readiness authority.

The completed post-foundation classification already inspected it read-only and independently concluded `0.7.0` is not ready.

### `parallel/prompt-packaging-integrity-audit`

Classification:

`ISOLATED_NONCONTROLLING`

It contains a historical parallel prompt document and is substantially behind current `master`. Do not merge or update it as part of current workflow work.

## 9. Open Pull Request #2

PR:

`Add launcher asset contract and sidebar metadata`

Classification:

`ISOLATED_NONCONTROLLING`

Current concerns:

- branch base is substantially behind current `master`;
- the PR includes UI source changes and new SVG assets;
- the PR body says tests were not run;
- its original behavior-preserving claims must be reverified against current shell, asset, accessibility, and route behavior;
- it must not be merged as a connector-only documentation pass.

Recommended later disposition:

Run a dedicated read-only PR applicability audit. If still desired, recreate the smallest accepted changes from current `master` and validate locally rather than merging the stale branch wholesale.

## 10. Cleanup Candidates Requiring Later Reference Checks

The existing fold-ready proposal identifies these candidates:

- `docs/design/economy-clarity-audit.md`;
- `docs/dev/economy-runtime-test-failure-triage-plan.md`;
- `docs/design/chronicle-run-end-summary-source-audit.md`.

Before any deletion:

1. search all current references;
2. classify each reference as active, supporting, or historical;
3. verify the successor document contains every still-needed owner and boundary fact;
4. fold compact historical facts where necessary;
5. delete no more than one or two files in a dedicated cleanup run;
6. do not combine cleanup with active capability work.

## 11. Recommended Maintenance Sequence

### Pass A: connector prep index refresh decision

Decide whether to refresh `current-prep-index-and-codex-source-stack.md` or replace it with a timeless source-family index.

### Pass B: historical audit reference check

Recheck the three fold-ready candidates and refresh `historical-audit-doc-index.md` only after exact current references are known.

### Pass C: temporary research-artifact retention inventory

Produce a no-deletion ledger for every surviving `tmp-*` artifact.

### Pass D: stale isolated branch and PR disposition

Record keep, recreate, close, or delete recommendations without merging historical branches.

### Pass E: optional complete-file maintenance

Only after the evidence passes above, update stale execution sections in one bounded documentation maintenance run. Do not mix with implementation or active prompt changes.

## 12. Relationship To The Active Route

The active route is the unversioned `Ashen Reef Survey Activity Advancement Scope And Owner Contract Decision`.

This freshness audit is parallel workflow evidence only. It does not alter the current prompt, selected capability, version classification, or implementation posture.

## 13. Non-Implementation Confirmation

This pass:

- creates one documentation file only;
- changes no current prompt, handoff, output, roadmap, sequence, backlog, planning anchor, or static-content program;
- changes no source, content, schema, validator, test, engine, app, shared contract, save, migration, dependency, generated output, asset, UI, or gameplay path;
- deletes, folds, merges, rebases, or closes nothing;
- authorizes no implementation or cleanup.
