# Version 0.7.0 - Integrated Gameplay Systems Band Entry

Date: 2026-08-27

Label class: primary band-entry milestone

Milestone impact: `band_entry_candidate`

Execution posture: synchronized repository-first milestone activation/publication; production, schemas, content, tests, saves, migrations, dependencies, assets, and gameplay behavior are read-only

Accepted readiness decision: `docs/design/integrated-gameplay-0.7-band-entry-readiness-decision.md`

Readiness result: `BAND_ENTRY_READY`

Accepted representative authority: `Version 0.6.11.1 - Ashen Reef Survey Ordinary Reachability And Representative Loop Acceptance Audit`

## Objective

Activate and publish the reserved `Version 0.7.0 - Integrated Gameplay Systems` internal maturity milestone over the already accepted bounded representative loop. Reverify that the live head has not invalidated the accepted readiness matrix, run the smallest focused executable confirmation, and update the durable route so `0.7.x` is the current maturity band.

This is a bounded milestone integration/acceptance package, not a new feature package. Do not add gameplay behavior merely to make the version look substantive. The durable milestone decision itself qualifies this documentation-only primary because it closes the accepted band-entry gate and governs the next capability sequence.

Return exactly one result:

- `MILESTONE_ENTRY_ACCEPTED`; or
- `MILESTONE_ENTRY_BLOCKED`.

## Required Orientation

Follow `AGENTS.md`, the repository-first protocol, Codex-versus-Connector handling procedure, platform/tool policy, resource-slicing policy, branch policy/register, and applicable failure-pattern register. Fetch/prune, synchronize clean `master`, inventory live branches and pull-request refs, and distinguish accepted implementation, readiness-decision, inspected, coordination, pushed, tracking, and hosted heads.

Read completely:

- current prompt, handoff, output, historical register, and planning reconciliation;
- `docs/design/integrated-gameplay-0.7-band-entry-readiness-decision.md`;
- the internal versioning and release-milestone policy;
- accepted `0.6.9`, `0.6.10`, `0.6.11`, and `0.6.11.1` authorities;
- current representative source/caller/persistence/test paths and the complete delta since the readiness decision.

Connector packets and protected/evidence refs remain supporting evidence only.

## Acceptance Gate

Use `MILESTONE_ENTRY_ACCEPTED` only if all of the following remain true at the synchronized live head:

1. no post-readiness production or tracked-test drift invalidates any criterion;
2. the ordinary creator -> publication/load -> quest acceptance/access -> travel/arrival -> four survey shifts -> restart -> durable duplicate path remains executable;
3. engine-owned commands/results/events, campaign admission, persistence, typed consequences, stale/conflict rejection, correction/repair, nested-owner preservation, and accepted-only real-caller application remain coherent;
4. Soundings remains active and unturned-in, with no legacy turn-in/reward behavior promoted into milestone canon;
5. the focused current-head validation passes;
6. every documented omission remains either outside the representative interaction or assigned to a later maturity band;
7. no new mandatory blocker is found.

If any criterion fails, return `MILESTONE_ENTRY_BLOCKED`, do not record `0.7.0` as accepted/current, identify the exact failed criterion and smallest coherent `0.6.x` repair or decision, and fail closed.

## Required Evidence

- Inspect the complete diff from readiness-decision source head `dc89c8f0421e3e657740f03ecfa611a29ae2f8b3` to synchronized live head.
- Reinspect the real creator, new-campaign attempt/publication, quest acceptance/access, travel/arrival, survey owner, campaign admission, real caller, accepted-only session bridge, persistence, and representative test.
- Run the representative ordinary-reachability test plus the focused survey-command, survey-persistence, and campaign-persistence tests. Expand only if drift or failure requires it.
- Confirm the representative test imports no `demoSnapshot`, injects no eligibility state, ends with the quest active/unturned-in, and asserts the durable duplicate and exact authority graph.
- Apply at minimum `FP-001`, `FP-002`, `FP-008`, `FP-009`, `FP-013`, `FP-014`, and `FP-017`.

## Coordination And Publication

On `MILESTONE_ENTRY_ACCEPTED`:

- record `Version 0.7.0 - Integrated Gameplay Systems Band Entry` as complete and accepted;
- set the current maturity band to `0.7.x` without claiming `0.8.0` readiness;
- preserve the accepted representative-loop boundary and all later-system deferrals;
- install one separate next route chosen under the normal owner/version classification rules, without implementing it in this run;
- update current prompt/output/handoff, the readiness decision, historical register, planning reconciliation, branch register, and lower-precedence live headers;
- commit only intended documentation, push `master`, fetch/prune, verify `HEAD == origin/master`, retrieve hosted prompt/output/handoff, and finish clean.

On `MILESTONE_ENTRY_BLOCKED`, update the same coordination surfaces with the fail-closed result and smallest current-band repair route.

## Scope Exclusions

Do not modify production, shared contracts, schemas, serializers, migrations, tracked tests, content, dependencies, assets, UI, or saves; implement quest turn-in/rewards; perform class/progression cleanup or attribute rebalancing; create generic quest/travel/activity/event/effect infrastructure; migrate travel keys; add inventory instances, NPC promotion, other Stakes modes, `0.8.x` slice hardening, packaging, or release work; or mutate evidence/protected branches or pull requests.
