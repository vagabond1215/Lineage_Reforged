# Ashen Reef Survey Ordinary Reachability And Representative Loop Dependency Closure Decision

Date: 2026-08-13

Label class: unversioned documentation-only decision

Milestone impact: `supports_current_band`

Execution posture: repository-first, docs-only decision with read-only production inspection and removable probes

Accepted parent: `Version 0.6.10 - Ashen Reef Survey Advancement Authority`

Parent acceptance authority: `950e851446fb75bfbdb717d0ea33e33ec2907d4a`

Starting representative classification: `REPRESENTATIVE_LOOP_EVIDENCE_INCOMPLETE`

## Objective

Decide the smallest coherent authority and implementation package that makes the accepted Ashen Reef survey loop ordinarily reachable from production character creation and proves it through one injection-free creator-to-restart representative path.

Return exactly one outcome:

- `PACKAGE_READY` only if current repository authority can settle every required quest-offer, known-location, travel, activity, persistence, and evidence boundary without inventing canon or weakening accepted owners;
- `NO_PACKAGE` if any material product/canon/ownership choice remains unresolved. Install only the smallest decision or authored-input prerequisite needed to resolve it.

Do not implement production or tracked tests in this decision.

## Starting Disposition

- parent `0.6.10`: `PARENT_ACCEPTED` through independent `0.6.10.5`;
- representative loop: `REPRESENTATIVE_LOOP_EVIDENCE_INCOMPLETE`;
- ordinary production creator state: no quest-journal row, no tracked quest, only the starting settlement known, and no Ashen Reef activity record;
- direct entry failures after production version-7 publish/load: `quest_missing`, `destination_not_known`, `activity_missing`, and `survey_quest_missing`;
- survey turn-in/rewards and geographic Knowledge/map proposals: excluded;
- `0.7.0`: `NOT_READY`.

## Authority And Orientation

Read `AGENTS.md` completely and follow the repository-first protocol, prompt-execution platform/tool policy, branch policy/register, and applicable failure-pattern register, especially `FP-001`, `FP-002`, `FP-005`, `FP-008`, `FP-009`, `FP-011`, `FP-013`, `FP-014`, and `FP-017`.

Read the complete current prompt, handoff, output, historical register, planning reconciliation, permanent survey acceptance audit including the `0.6.10.5` appendix, survey receipt decision, activity scope/owner decision, minimum save/publication decision, accepted quest acceptance/tracking, travel, activity selection, survey advancement, and Normal persistence/recovery authorities. Reconcile the protected integrated-gameplay readiness evidence against current source rather than adopting its historical implementation claims.

Fetch/prune and synchronize clean `master`. Record the accepted parent implementation and repairs, parent audit authority, coordination head, decision starting head, decision/coordination commits, pushed remote head, and post-fetch hosted head distinctly.

Use multi-agent work only for bounded, separable read-only inspection. The primary agent must make the package decision and reverify every material subagent claim against the synchronized checkout.

Inventory all local/remote branches and open pull requests. Preserve all registered evidence, superseded, and protected refs; no branch/PR mutation is authorized unless a fresh controlling lifecycle trigger proves it due.

## Decision Boundary

This run may change only documentation and removable temporary probes. Do not modify production source, shared contracts, tracked tests, schemas, serializers, migrations, formats, dependencies, content, assets, UI, or behavior.

Do not infer authored setting facts from demo fixtures, display prose, role labels, quest strings, generated operators, or the protected readiness proposal. If the repository does not decide who offers the quest or how Ashen Reef becomes known, return `NO_PACKAGE` with the smallest authored-input or owner decision.

## Required Production Reachability Map

Trace the actual production path end to end:

1. ordinary `createNewGameSnapshot(...)` creation and target initialization;
2. initial version-7 publication and load;
3. quest availability/offer creation;
4. quest acceptance and tracking;
5. Ashen Reef known-location or travel-access acquisition;
6. accepted travel to `location.ashen_reef`;
7. survey activity activation and, if required, explicit activity selection;
8. all four accepted survey shifts through the production caller and accepted-only session application;
9. publication/load or restart at the material boundaries required by current authority;
10. final retained result, duplicate, projections, quest progress, operation/activity, and correction/repair posture.

For every edge, identify:

- current owner and callable production seam;
- source facts and eligibility trigger;
- stable identity, stale/retry, duplicate, rejection, and atomicity behavior;
- persistence and restart posture;
- whether the edge exists, is merely fixture/demo data, or requires a new bounded owner;
- exact tests that currently prove it or the missing representative assertion.

## Required Decisions

### 1. Quest Offer Authority

Decide the authoritative source for the `quest.ashen_reef_survey` offer, when it becomes available, and how it enters `sessionState.questJournal` before the accepted quest command can consume it.

Distinguish authored quest definition, runtime offer instance, journal projection, acceptance command, and tracking. Do not seed every demo quest into every new campaign merely to satisfy a test.

### 2. Known Location And Travel Access

Decide who establishes `location.ashen_reef` as known and at which accepted occurrence. Preserve the survey result's exact `knownLocationAuthority`, `mapAuthority`, `travelAccess`, and `geographicKnowledge` `no_proposal` fields: survey advancement itself must not become the missing access owner.

Do not equate a Codex row, quest prose, map presentation, settlement identity, or geographic Knowledge with travel admission unless an accepted owner explicitly does so.

### 3. Activity Activation Boundary

Reconcile the existing travel-owned Ashen Reef arrival hook with engine-owned activity selection and survey readiness. Decide whether successful tracked-quest travel may author the survey current activity/operation directly under its existing accepted contract, whether a distinct activity record/selection edge is required, or whether a narrower owner transition is needed.

Do not create a generic activity framework.

### 4. Persistence And Retry Order

Fix the minimum publish/load/restart checkpoints for the representative path. Preserve version-7 campaign authority, continuity-before-receipt admission, current-head duplicate semantics, technical-retry identity, projection-only repair, correction pending blocks, and Normal defeat/recovery preservation.

### 5. Representative Test Contract

Specify one injection-free repository test that starts from the ordinary creator and obtains every prerequisite through production owners. It must exercise the real caller or extracted production coordinator, all four shifts, accepted-only state, persistence/restart, exact duplicate behavior, and the nine-field `no_proposal` boundary.

The test may use deterministic user choices and controlled storage, but it may not insert the survey quest, tracked posture, known location, survey activity, sector flags, discovery, or owner receipts directly.

### 6. Package Classification

If all choices are repository-decided, define the smallest current-band implementation package, exact production/test/doc surface, transaction ordering, failure matrix, validation commands, and exclusions. The provisional label is:

`Version 0.6.11 - Ashen Reef Survey Ordinary Reachability And Representative Loop Evidence`

Use that label only if the package materially adds the missing bounded production capability and satisfies the internal version policy. Otherwise choose a smaller support or unversioned prerequisite and explain why.

## Required Evidence And Validation

Use read-only source inspection plus removable probes to establish:

- ordinary creator and production version-7 publication/load facts;
- absence or presence of a production quest-offer writer;
- absence or presence of a production known-location/access writer;
- travel arrival-hook behavior with a genuinely acquired tracked quest;
- activity activation/selection behavior;
- four-shift caller/session/persistence feasibility without injected eligibility;
- exact scope and current counts of focused tests that would become the representative matrix;
- public export/mirror, browser storage, migration, and campaign-ledger seams affected by any proposed package;
- clean worktree, diff hygiene, branch/upstream, remote head, hosted files, status, and workflow availability.

Do not run broad validation that cannot affect this documentation decision. Do not treat a green fixture-based survey suite as reachability evidence.

## Outcome Rules

### `PACKAGE_READY`

Use only if every authority choice is already decided. Create a durable focused decision with:

- exact owner graph and transition order;
- exact source and test surface;
- request/result/event or reuse decision for each new edge;
- save/restart and failure matrix;
- exclusions and rollback boundary;
- version classification and exact next implementation prompt.

Keep `0.7.0` `NOT_READY`; only a later successful implementation, independent acceptance, and explicit docs-first band-entry decision may change it.

### `NO_PACKAGE`

Use if quest canon, offer timing, access provenance, or another material owner choice is unresolved. Record the blocking question, evidence already exhausted, unsafe inference to avoid, exact user/authored input required, and smallest follow-up decision. Do not install an implementation prompt.

## Required Coordination And Publication

Update current prompt/output/handoff, the permanent survey acceptance record when necessary, repository-first Current Application, historical register, planning reconciliation, roadmap, sequenced plan, continuity brief, backlog, static program, failure-pattern register when warranted, and branch register. Preserve dated history and exact supersession language.

Commit only intended documentation and removable-probe cleanup, push `master`, fetch again, verify `HEAD == origin/master`, retrieve hosted prompt/output/handoff, inspect hosted status/check/workflow evidence, and finish with a clean worktree.

## Scope Exclusions

Do not implement reachability in this decision; implement survey turn-in/rewards; grant currency, standing, inventory, reputation, or General Lore turn-in rewards; change survey balance or four-stage behavior; add geographic Knowledge, map, recognition, fog, generic discovery, or generic travel-access behavior; add generic quest/activity/effect/transaction/event/replay/correction infrastructure; add other Stakes modes, checkpoint/cloud/death/succession work; add or alter authored canon without explicit authority; change dependencies, formats, content, assets, generated output, or unrelated UI/runtime; or mutate/integrate/close/delete/rebase/force-update branches or pull requests.
